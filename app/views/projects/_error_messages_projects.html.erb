<% if @project.errors.any? %>
     <div>
          <p><%= pluralize(@project.errors.count, "error") %> prevented this post from saving.</p>
          <ul>
               <% @project.errors.full_messages.each do |msg| %>
               <li><%= msg %></li>
               <% end %>
          </ul>
     </div>
<% end %>
